{
  "name": "blank-page",
  "version": "1.0.0",
  "description": "A pure white blank page optimized for desktop",
  "main": "index.html",
  "scripts": {
    "start": "npx serve -s . -p 8080",
    "dev": "npx serve -s . -p 8080",
    "build": "echo 'Build successful - static files ready' && exit 0"
  },
  "devDependencies": {
    "serve": "^14.2.0"
  },
  "keywords": ["blank", "page", "static"],
  "author": "",
  "license": "MIT"
}
